import { styled } from "@mui/material";

const FooterHeader = styled("h5")(({ theme }) => ({
  fontSize: "1.66rem",
  fontWeight: 900,
  letterSpacing: "-1px",
  lineHeight: "0",
  margin: "2rem 0",
  padding: 0,
}));

export default FooterHeader;
