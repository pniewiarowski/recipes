import { styled } from "@mui/material";

const FooterContainer = styled("div")(({ theme }) => ({
  overflow: "hidden",
  borderRadius: "0",
  backgroundColor: "#000",
  padding: "2rem 1rem",
}));

export default FooterContainer;
