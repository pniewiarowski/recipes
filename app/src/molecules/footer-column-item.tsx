import { styled } from "@mui/material";

const FooterColumnItem = styled("h6")(({ theme }) => ({
    fontSize: "1.33rem",
    fontWeight: "200",
    lineHeight: "2.33rem",
    margin: 0,
    padding: 0,
}));

export default FooterColumnItem;
