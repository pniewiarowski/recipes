import { styled } from "@mui/material";

const FooterColumnHeader = styled("h6")(({ theme }) => ({
    fontSize: "1.66rem",
    fontWeight: 900,
    lineHeight: 0,
    margin: "3rem 0 1.5rem 0",
    padding: 0,
}));

export default FooterColumnHeader;
